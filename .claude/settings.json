{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=$(echo $TOOL_INPUT | jq -r '.file_path // empty') && if [[ \"$filepath\" == *.go ]]; then gofmt -w \"$filepath\" 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
